<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Volume 5 Chapter 5.11 §1's Android foreground service.

         Only ONE permission is declared here. flutter_foreground_task's own
         manifest already merges FOREGROUND_SERVICE, WAKE_LOCK,
         POST_NOTIFICATIONS and RECEIVE_BOOT_COMPLETED into this application,
         so re-declaring them would state the same thing twice and let the two
         copies drift.

         FOREGROUND_SERVICE_DATA_SYNC is the exception: Android 14 requires the
         permission matching the service's declared foregroundServiceType, and
         the plugin cannot know which type a consumer picked. dataSync is the
         type Google documents for "transfer data between a device and the
         cloud over a network", which is exactly what Chapter 5.10's pipeline
         does.

         The service element below is required and its android:name must not
         change - the plugin resolves that exact class. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Chapter 5.11 §1's long-running upload service.

             android:name is the plugin's own class and must be spelled exactly
             this way. android:exported="false" because nothing outside this
             application ever starts it - the dispatcher does, in process. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
